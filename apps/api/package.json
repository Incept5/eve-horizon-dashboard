{
  "name": "eve-horizon-api",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "build:client": "cd client && npm install && npm run build && cp -r dist ../dist",
    "build": "npm run build:client",
    "test": "node --test"
  }
}
